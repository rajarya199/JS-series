memoization is optimizations technique  that make application more effiecient and faster
It store function result in cache and return if same function getting call again and again
itshould be used with function which take more time and memeory in execution